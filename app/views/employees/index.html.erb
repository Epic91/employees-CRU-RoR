<h1>All employees</h1>

<% @employees.each do |employee| %>
    <ul>
        <li><%= link_to employee.first_name, employee_path(employee) %> | <%= employee.occupation %></li>
    </ul>
<% end %>

<%= link_to 'Add new employee', new_employee_path %>